# Changelog

EdgeChat 从 `2.1.2` 起正式采用语义化版本。更早的版本号依据已经完成的主要里程碑补记，用于说明项目演进，不对应历史 Git 标签。

## 2.8.1 - 2026-09-21

- 群聊消息发送者姓名旁新增 Telegram 风格头衔：站点管理员显示绿色“管理员”，永久或仍在有效期内的封禁账号显示红色“禁言中”，封禁头衔优先。
- 群成员面板复用同一头衔组件与状态规则；生产 API 和 demo 同步投影管理员、封禁状态，不新增数据库迁移或云资源，不修改 Android。

## 2.8.0 - 2026-09-19

- 新增后台独立「跨实例群组绑定」页面及三语言左侧栏入口；公开、私有与 general 群可一对一绑定，短期邀请、对端认领与发起方最终确认后双向同步新纯文字。
- 协议使用独立实例/绑定身份、每绑定长期凭据、HTTPS HMAC 请求与确认签名、目标群及 revision 世代校验；凭据分别由本站 keyring 加密，不交换主密钥、不创建远端登录账号。
- 消息事务原子登记持久化 outbox；接收 receipt 与消息同事务写入且独立于消息删除。按需 DO alarm 加低频补偿 cron，支持有界退避、超时、积压上限、过期清理及可见失败状态。
- 支持暂停、恢复和解绑；暂停清空积压，恢复不补发旧世代，解绑撤销新收发。群内显示绑定关系与来源实例；附件、历史、引用关系、编辑及撤回不跨实例同步。
- Telegram 与跨实例出站统一只接受本站原创，禁止外部桥接消息继续跨桥传播。Telegram 既有尽力转发/重试限制保持不变。
- 新增 D1 迁移 `2026-09-19-instance-bridge`、`INSTANCE_BRIDGE` DO v3 和 15 分钟全局救援 cron；部署模板、维护自检及说明文档同步更新，不修改 Android。

## 2.7.3 - 2026-09-19

- 网页首次访问继续只依据浏览器首选语言，不使用 IP 或地理定位：`Hant`/旧式 `CHT` 使用繁体，`Hans`/旧式 `CHS` 使用简体；未声明脚本时，台湾、香港、澳门使用繁体，其余中文地区与裸 `zh` 使用简体，非中文继续使用英文。
- 明确脚本优先于地区，例如香港用户选择 `zh-Hans-HK` 时使用简体、中国大陆用户选择 `zh-Hant-CN` 时使用繁体；用户在语言菜单中的手动选择仍持久化并优先于自动识别。
- 使用当前 demo 重新截取 README 的聊天界面与管理后台预览图，Telegram 桥接演示图保持不变。
- 本次调整网页语言检测、测试与 README 预览图，不改动 Android、GitHub Actions、数据库结构或云资源。

## 2.7.2 - 2026-09-16

- 修复 D1 UTC 时间缺少时区标记时被浏览器误当成本地时间的问题，网页消息时间及其他服务端日期现在会按客户端浏览器时区正确显示。
- 本次仅调整网页共享日期解析与测试，不改动 Android、GitHub Actions、数据库结构或云资源。

## 2.7.1 - 2026-09-15

- 修复 Capacitor Android CI 初始化阶段请求已下架 Android SDK `tools` 包的问题，显式仅安装仍可用的 `platform-tools` 后再安装既有 SDK 36；不改动 Android 源码或功能。

## 2.7.0 - 2026-09-15

- Telegram 桥接由仅支持公开群组扩展为支持公开与私有群组，管理员可在同一后台列表中查看群名、区分群类型并统一配置映射。
- 私有群的 Telegram 入站、出站、附件、回复、实时广播与未读投影复用现有群聊链路；一对一私信继续保持不可映射。
- 同步更新生产与 demo 契约、三语言界面和部署文档；不新增数据库迁移、云资源或 Android 原生改动。

## 2.6.2 - 2026-09-15

- 新增繁体中文界面，右上角语言入口改为简体中文、繁体中文与 English 三项菜单，并按当前语言异步加载独立语言包。
- 浏览器语言识别区分简繁中文，语言选择继续持久化；服务端错误、日期格式与富文本编辑器语言同步跟随当前界面。
- 收敛成员角色、管理员名称和通用请求失败等散落的可见硬编码，不新增数据库迁移、云资源或 Android 原生改动。

## 2.6.1 - 2026-09-13

- 重塑网页聊天工作区视觉层级：使用浅灰绿画布、白色栏面与深绿主操作，统一 44px 触控控件、80px 会话和通讯录行，并强化选中与未读状态。
- 优化桌面和窄屏聊天布局：输入框在 480px 以下独占首行，工具栏移至次行；成员面板改为覆盖层，空会话增加明确引导，消息气泡与时间戳留白同步调整。
- 本次仅调整 Vue Web 样式与视觉契约，不新增数据库迁移、云资源或 Android 原生改动。

## 2.6.0 - 2026-09-13

- 新增所有已登录用户可用的站内通讯录，默认仅展示头像和姓名，支持姓名/用户名搜索，并按需复用既有资料卡与私信打开流程。
- 聊天与通讯录共享持久工作区和唯一 inbox 连接；切换时暂停隐藏房间连接，保留搜索、滚动位置与未发送草稿，不新增好友关系、数据库迁移或云资源。

## 2.5.0 - 2026-09-12

- 网页群聊与私聊正文统一支持安全 Markdown，保留结构化提及、HTTP(S) 自动链接、纯文本摘要和 Markdown 原文复制。
- 消息输入栏可原位展开 Vditor WYSIWYG 编辑器；完整编辑器、专用样式与自托管运行时仅在首次点击后加载，并继续复用原草稿、附件、回复和发送链路。
- 原始 HTML、远程图片和动态媒体预览在编辑阶段即安全降级，不新增后端接口、数据库迁移或云资源；本次按需求仅完成本地实现与验证，未部署。

## 2.4.0 - 2026-09-12

- 新增站内可见的纯文本个人简介与可复用用户资料卡，支持 Web 设置编辑、消息/成员/私信头像入口和移动浏览器底部面板。
- 资料按需读取，外部身份独立展示；复用既有私信与拉黑链路，完善部分更新、请求竞态和弹层焦点管理。
- 升级须先执行 `2026-09-12-user-bio` D1 迁移；GitHub Actions 自动迁移和维护自检清单已接入，无新增云资源。

## 2.3.0 - 2026-09-12

- 新增网页端分层消息通知：前台其他会话使用 Telegram Web 风格右下角 Toast，前台当前会话仅在提及或回复时提示，后台或最小化继续使用系统通知。

## 2.2.1 - 2026-09-11

- 修复刷新后私信顶栏丢失拉黑状态、需要重复点击才能解除拉黑的问题。

## 2.2.0 - 2026-09-11

- 新增私信用户拉黑与解除拉黑，服务端统一阻断双方发送，并向被拉黑者返回明确拒绝提示。

## 2.1.2 - 2026-09-08

- 统一 Web、安装维护页、demo 与 Capacitor 客户端的项目版本来源。
- 完成安装维护自检、Cloudflare 资源隔离与二代客户端发布后的稳定性整理。

## 2.1.1 - 2026-09-06

- 隔离并精确复用 Cloudflare KV 会话命名空间，完善双账户部署校验。

## 2.1.0 - 2026-09-05

- 新增网页安装与维护自检、D1 迁移账本校验和诊断报告。

## 2.0.0 - 2026-09-05

- 以复用 Vue 界面的 Capacitor 客户端作为主要 Android 发行版，进入第二代产品线。

## 1.2.0 - 2026-09-04

- 完成群聊提及、语音消息与 Telegram 风格消息回复。

## 1.1.0 - 2026-08-31

- 增加 Telegram 双向同步、移动 API v1 与首版 Android 客户端。

## 1.0.0 - 2026-04-05

- 建立 EdgeChat 的 Cloudflare Worker、D1、KV、R2 与 Vue Web 基础版本。
